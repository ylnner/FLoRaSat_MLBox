package Nodes._40_Server._60_Mobility;

module ServerMobility
{
    parameters:
        double latitude @unit(deg) = default(45deg);
        double longitude @unit(deg) = default(5deg);
        @display("i=block/cogwheel");
}
